import { TemplateToSlotPipe } from './template-to-slot.pipe';

describe('TemplateToSlotPipe', () => {
  it('create an instance', () => {
    const pipe = new TemplateToSlotPipe();
    expect(pipe).toBeTruthy();
  });
});
